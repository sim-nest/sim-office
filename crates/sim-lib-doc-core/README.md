# sim-lib-doc-core

Core document records and shape hooks for SIM office documents.

The crate defines the shared document model: open kind names, stable ids,
opaque runtime bodies, source-system references, and a shape value for each
document kind. It also embeds descriptor recipes so the document core is visible
through the cookbook catalog.
