# Evidence fact graph

Describes a local document store that records evidence as subject, predicate,
and external-reference object rows. The linked records stay reference-only, so a
mail message, SharePoint file, Dalux item, schedule task, or ledger voucher can
support the same office subject without storing private payloads.
