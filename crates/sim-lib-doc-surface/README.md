# sim-lib-doc-surface

Scene projection and intent decoding for SIM office document panes.

The crate turns office documents into baseline Scene values that existing view
hosts can render. It also decodes validated Intent values into open document
edits, so a suite pane can preview changes before any live API adapter runs.
