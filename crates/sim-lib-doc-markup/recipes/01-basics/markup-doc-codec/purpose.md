# Markup document codec

Shows a markup-backed article crossing the office document codec boundary:
Markdown bytes decode to an article document, and the same markup body encodes
through the Typst backend.
